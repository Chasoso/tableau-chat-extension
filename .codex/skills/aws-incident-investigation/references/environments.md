# Environments

- Default allowed environment: `dev`
- Do not search staging or production unless a separate reviewed change says so
- Use the repository-documented timezone default when the user omits one

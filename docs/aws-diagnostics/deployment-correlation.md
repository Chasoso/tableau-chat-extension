# Deployment Correlation

Incident investigation should correlate the active deployment with the
structured log stream.

Use:

- deployment commit or version
- `requestId`
- `correlationId`
- component
- operation
- time window

Keep the evidence summary redacted and concise.
